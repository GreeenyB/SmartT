# SmartT UI

Standalone dashboard files for the SmartT fuel intelligence interface.

Open the dashboard file directly in a browser:

```text
dashboard/index.html
```

The interface uses plain HTML, CSS, and JavaScript with no external assets beyond the local SmartT logo files.
